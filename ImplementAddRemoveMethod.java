import java.util.Arrays;

public class ImplementAddRemoveMethod 
{
    int index = 0;
    public  void add(int userIndex , int data) 
    {
        if(userIndex > index || userIndex < 0) 
       // throw new ArrayIndexOutOfBoundException();

        for(int i =userIndex; i<index; i++)
        {
        //    a[i] = a[i+1]
        }


    }
}
